<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disabled -->
    <item android:alpha="10" android:color="@color/fair_gray" android:state_enabled="false"/>

    <!-- Focused -->
    <item android:color="@color/secondary" android:state_focused="true"/>

    <!-- Hovered -->
    <item android:color="@color/fair_gray" android:state_hovered="true"/>

    <!-- Normal -->
    <item android:color="@color/fair_gray"/>
</selector>